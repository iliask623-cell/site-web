# Copie ce fichier en .env.local et renseigne les valeurs de ton projet Supabase
# (Project Settings > API dans app.supabase.com).
# Si ces variables sont absentes, l'application tourne en mode démo (données en mémoire/localStorage).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
